# mcp-bridge

A small collection of read-only MCP servers that fill gaps in the official MCP servers from various services. Self-hosted on Vercel. Currently:

- **`/notion`** — query Notion data sources (filter/sort/paginate) and saved database views, on plans below Business where the official Notion MCP gates these tools.

Full setup instructions to follow.
